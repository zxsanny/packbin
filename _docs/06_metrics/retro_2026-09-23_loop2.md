# Retrospective — loop 2

**Date**: 2026-09-23
**Paths**: `_docs/03_implementation/`, `_docs/06_metrics/`, `_docs/02_tasks/`

## Implementation

| Metric | Value |
|--------|-------|
| Tasks | 4 (AZ-1938, AZ-1939, AZ-1940, AZ-1941), all Done |
| Batches | 4 |
| Tasks per batch | 1 |
| Complexity points | 18 (3 + 5 + 5 + 5) |
| Points per batch | 4.5 |
| Blocked tasks | 0 |
| First-attempt batches | 4 |

## Quality

| Metric | Value |
|--------|-------|
| Reviews | 4 |
| PASS | 1 (batch 4) |
| PASS_WITH_WARNINGS | 3 (batches 1–3) |
| FAIL | 0 |
| Critical / High / Security | 0 |
| Medium Maintainability | file length over 500 lines, repeated on the packers and their tests |
| Architecture delta | 0 new, 0 resolved |

## Structure

Six packages. Cross-component imports 0. Cycles 0. Unchanged from loop 1. See `structure_2026-09-23_loop2.md`.

## Trends

Loop 1's retrospective kept the packages as peers. This loop added string, list, and dictionary inside each package and did not add a shared walker. The file-length warnings grew because those arms stayed in the existing packer files. Batch 4's review was clean because the handoff drivers are separate programs.

The C# handoff and the position driver shared one `obj` directory until their output paths were split. The Mac C++ compile needs the SDK headers.

## Actions

1. Split pack and unpack in the files already over 500 lines before the next field kind lands.
2. Give each extra C# executable its own `BaseOutputPath` when it shares a folder with another `Main`.
3. Keep cross-language proof as producer bytes fed to the consumer.

## Compared with loop 1

Peer packages and the one-second suite bound held. File-length warnings are the new repeat. Publish is still a `v*` tag, not a laptop deploy script.
