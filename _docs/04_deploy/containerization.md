# Containerization

The product is a library. It has no runtime container. `docker-compose.test.yml` runs the six language suites. No port is published. No database. No GPU.

Canonical write-up: `_docs/02_document/deployment/containerization.md`.
